---
name: agentic-commerce-watch-refresh
description: Daily 8am refresh of the Agentic Commerce & Payments watch artifact: adds/removes articles and figures, keeps links verified, maintains the Timeline and badge-new system.
---

You maintain the "Watch - Agentic Commerce & Payments" site. It now lives as SPLIT SOURCE FILES in a local git repo, not as one giant HTML blob — read this whole section before doing anything.

## Where everything lives (local repo: `C:\Users\FU039NCE\OneDrive - Fime SAS\Documents\Veille\AgenticMonitoring`)
- `src/pages/<id>.html` — one file per site section. IDs and what's in them: `home`, `fortnight` (This Fortnight), `timeline`, `network` (Network & Payments), `crypto` (Emerging Rails & Stablecoins — yes, the folder/id is still "crypto", the nav label is "Emerging Rails & Stablecoins"), `commerce`, `market`, `research`, `linkedin`, `protocols`, `perplexity`, `fact`, `atlas`, `archive`.
- `src/style.css` — all CSS. Essentially never changes.
- `src/script.js` — all JS (hash routing, More dropdown, Timeline expand/filter). Essentially never changes.
- `src/assets/*.png` / `*.jpg` — real image files (logos, hero banner, favicons). Referenced from the page fragments as `src="assets/filename.png"`.
- `src/head.html`, `src/shell-top.html`, `src/footer.html`, `src/tail.html`, `src/page-order.json` — the shell/scaffolding around the pages (topbar+nav, footer, head meta, closing tags, and the array defining page order). You should rarely if ever need to touch these.
- `build.js` — run with `node build.js` from the repo root. Reassembles everything in `src/` into two generated files: `index.html` (full document, real `assets/...` file references, this is what GitHub Pages serves) and `dist-artifact.html` (single self-contained fragment, images re-inlined as base64, this is what gets published to the Claude Artifact).
- There is no `routines/` backup copy of this SKILL.md in the repo anymore (it was intentionally removed) — this scheduled task's prompt (edited via `update_scheduled_task`) is the only copy. Don't try to read or write a `routines/` path.
- This routine is triggered manually by the user, not by a guaranteed daily cron — treat that as relevant context (see the research-window note in Steps each run below): there can be real multi-day gaps between runs.

## The daily workflow (replaces the old "fetch the live blob and edit it directly" approach)
1. `cd` into the repo above. Read the relevant `src/pages/<id>.html` file(s) for whatever you're about to touch — NOT the generated `index.html`/`dist-artifact.html`, edit the SOURCE fragments in `src/pages/`. This is the whole point of the split: you only need to load the small page(s) you're actually changing, not the whole site.
2. Make your content edits directly in the appropriate `src/pages/<id>.html` file(s) (add/remove cards, badges, etc. — same HTML patterns as before, just in a smaller file).
3. Run `node build.js` from the repo root. This regenerates `index.html` and `dist-artifact.html`. If it errors, stop and fix the underlying `src/` file before proceeding — do not hand-edit the generated files directly, your fix belongs in `src/`.
4. Sanity-check the diff: `git diff --stat` should show the `src/pages/<id>.html` file(s) you meant to touch, plus regenerated `index.html`/`dist-artifact.html`. If unrelated files changed, something's wrong — investigate before publishing.
5. Publish `dist-artifact.html` to the live Artifact: Artifact tool, file_path = `dist-artifact.html` in the repo above, `url` = `https://claude.ai/code/artifact/bf805b79-fc96-4a9c-9008-4b527cc33b46`, `favicon: "🛰️"`.
6. Commit to git: `git add` the changed `src/pages/*.html`, `index.html`, `dist-artifact.html`, then commit with a message describing what changed (same style as the existing git history: what was added/fixed and why). This keeps the local repo in sync with what's live — don't skip this step, it's now part of the routine's job, not a separate manual process.
7. GitHub Pages itself is NOT auto-pushed — `git push` is still the user's own call, same as before. Committing locally is enough for this routine; do not push to the remote.

## CRITICAL RULE on badge-new hygiene
Every run, before publishing, verify that old badges are removed: `badge-new` should only ever be on items that are genuinely new since the *previous* run's edition — added during THIS run. If something is still carrying the badge from a prior run and isn't today's addition, clear it. This was missed once already (a full prior edition's badges left in place a day later) — treat the check as a real, deliberate step, not something you assume happened automatically as a side effect of your other edits.

## CRITICAL RULE on description depth
Match the specificity of the "By major player" block on This Fortnight (e.g. naming the exact publication, its version/date, and what it concretely covers — not just a vague paraphrase). Applies site-wide, not just that block: when a generic secondary write-up is the easy find but a more specific, more current primary source exists (an official report with a version number, a dated press release, a named feature), dig for and cite that primary source instead. This especially applies to "By major player" entries, which should always reflect that player's most current genuinely significant move, not whatever was true when the entry was first written — every run, check each one for a more recent development the same way you'd check for a more precise date (see the CRITICAL RULE on dates below). This check is NOT limited to the research window in Steps each run below (that window is for finding new signals for the news-flow pages) — search back as far as needed to confirm whether a more recent significant move exists for that player, even if it's weeks or months old. Only replace an entry if what you find is a real, significant upgrade, not just newer-but-minor.

## CRITICAL RULE on link safety (distinct from the link-verification rule below)
Before adding any link, check it's not a security risk, not just that it resolves. If a browser/fetch tool or search result flags a URL with a malware, phishing, or security warning, drop that item entirely — don't add it, don't try to work around the warning, don't substitute another link from the same untrustworthy source. Only link to reputable, identifiable sources: established news/trade press, official company/protocol sites, arXiv/SSRN, recognized industry publications. Never use URL shorteners or link aggregators/redirectors that mask the real destination — always link the actual source.
NEVER save a `news.google.com/rss/articles/...` (or any Google News redirect) URL onto the site, even though the LinkedIn discovery step below uses Google News RSS to find items. That wrapper URL routes through a Google consent/cookie interstitial before reaching the real page — it's exactly the kind of opaque redirect chain that trips security warnings, and it violates this rule. When a Google News RSS result is genuinely the item you want to add, do NOT copy the RSS article link — take the extra step to find and save the real destination URL instead (for LinkedIn items specifically: the actual `linkedin.com/posts/...` URL; for other news, the publisher's own article URL, e.g. via an additional WebSearch/WebFetch or by clicking through). If you cannot confidently confirm the real destination URL for a specific item, leave that item out rather than guess or fall back to the wrapper link. Note: as of Jul 2026 there is a known backlog of pre-existing Google News redirect links left over from before this rule existed, scattered mainly across the LinkedIn page and some This Fortnight rows — resolving that backlog is out of scope for the daily run (it was spun off as its own task), don't feel obligated to fix old ones you stumble across unless it's cheap to do (you already have the real URL in hand for another reason, as happened once with a misattributed "LinkedIn" story that was actually a Salesforce blog post).
If a source is completely unfamiliar and you can't independently verify it's a legitimate outlet, either find the same story reported by a known outlet instead, or leave the item out rather than risk linking somewhere unsafe.

## CRITICAL RULE on freshness (applies site-wide, prevents unbounded growth)
Unless explicitly exempted below, no page should carry a signal older than roughly 2 months from the current execution date — when adding new cards, also check existing ones on that page and retire anything that's aged out, so pages don't just grow forever. Exemptions: Timeline (deliberately a long-range historical spine back to Nov 2024, not subject to this window), FACT (evergreen framework page), Archive (deliberately preserves old dated snapshots), and Major protocols / Key actors (deliberately stable, slow-changing reference lists, not news feeds). This rule is the general reason This Fortnight (14-day window) and Research (2-month window) already prune — it now explicitly extends the same discipline to Network & Payments, Emerging Rails & Stablecoins, Commerce, Market, and LinkedIn, which previously had no stated pruning trigger.

## Floors, not targets (read first)
Every "at least N" or numeric minimum anywhere in this document (searches, LinkedIn feeds, Timeline cadence, etc.) is a BASELINE you must never fall below — it is not a target to stop at. The actual goal every run is to find what's genuinely necessary and most relevant, however much digging that takes. If you hit a minimum (e.g. did your 2-3 generic searches, checked both LinkedIn feeds) but sense there's more relevant material out there, keep going. Never treat "I did the minimum" as "I'm done" — treat it as "I've done the floor, now is there anything better/more relevant I haven't found yet."

## Minimum coverage rule (not everything is meant to be rare)
This Fortnight, LinkedIn, and the general news flow (Home "Top of the stack") should get at least some fresh signal on every run if at all possible — actively search for something to add here, don't treat them as "add only if something happens to turn up." Only conclude there's genuinely nothing new for these after a real search effort, not by default. This is different from Timeline and Key actors below, which are legitimately meant to change rarely (event-driven, not daily) — don't apply the same "go easy on it" logic to LinkedIn/news that's appropriate for Timeline.

## Anti-anchoring rule (read before researching)
Any company/protocol name mentioned anywhere in this document (here, in page descriptions, in the search-query list below) is an ILLUSTRATIVE EXAMPLE, never an exhaustive or preferred list. Do not let these names become the ceiling of what you look for — a new, smaller, or regional player doing something genuinely relevant belongs on the site exactly as much as Visa or Coinbase does. Every run, in addition to any named-entity searches, run at least 2-3 deliberately open/generic searches with NO company name in the query (e.g. "new agentic commerce startup funding", "new AI agent payment protocol launch", "stablecoin payment rail agent launch", "new checkout AI agent PSP") specifically to surface players not already on the site. If the same 10-15 brands are the only thing that ever gets added, that's a signal the routine has become anchored — actively look for who's missing. Don't artificially cap yourself at one new item per category either: if you find several genuinely new, verified signals in the window, add all of them, not just the first one you happened to check.

## Geographic coverage rule (distinct from the tag rule below)
This is a global watch, not a US-only one. English-language search results default heavily toward US/UK sources — actively counter that every run: include at least one search each for Asia (China: Alipay, WeChat Pay, UnionPay, Ant Group; also India: NPCI/UPI; Japan, Korea, Southeast Asia) and Europe (Adyen, Worldline, ECB/EU regulation, UK/EU banks) alongside the US-centric searches, not just when something happens to surface on its own. If a run's additions are exclusively US/UK companies several days running, that's a sign this rule is being skipped — go looking for the Asia/Europe signal you missed rather than assuming there wasn't one.

## Current structure (reproduce exactly, only content changes)
Top nav, in order: Home, This Fortnight, Timeline, Network & Payments, Emerging Rails & Stablecoins, Commerce, Market, Research, LinkedIn, Protocols, then a "More" dropdown (`.nav-more`) grouping Perplexity, FACT, ATLAS, Archive. Hash-routed single page (`location.hash`, `.page`/`.active`), default = home. Do not flatten the More dropdown back into the main bar and do not add/remove pages beyond this set without the user asking. If you ever do add/remove a page, also update `src/page-order.json` and `src/shell-top.html`'s nav links to match.
- Home (`src/pages/home.html`) has a hero banner image (`.hero-banner`) at the top, above the eyebrow line — intentional, part of the site's normal furniture. Preserve it; don't strip it, don't add a second one.
- **Network & Payments** (`src/pages/network.html`) = traditional card networks/PSPs/issuers/banks. Examples seen so far: Visa, Mastercard, Amex, Stripe, Adyen, Checkout.com, Nuvei, Thredd, Worldline, ING — this is not a closed list, any traditional network/PSP/issuer story qualifies even if the company has never appeared on the site before. Card-head tags use class `tag-reseau` but the label text should be the specific role ("Issuing", "Visa", "Mastercard", "PSP", "Identity" etc.), not always the literal word "Network".
- **Emerging Rails & Stablecoins** (`src/pages/crypto.html`) = crypto-native rails, blockchain settlement, stablecoin issuers. Examples seen so far: Coinbase, Circle, Binance, Ripple/x402, Tether, Solana Pay.sh — again not a closed list. Cards use class `tag-crypto` (outlined purple), label text = specific role ("Crypto", "Stablecoins", "Standards body", "Network"). If a story is genuinely both a traditional network AND crypto-native (e.g. Visa stablecoin strategy), it belongs here, not on Network & Payments — don't duplicate across both pages.
- **Timeline** (`src/pages/timeline.html`, after This Fortnight): a horizontal, click-to-expand milestone spine (`.tl-track`/`.tl-node`/`.tl-card`) from MCP (Nov 2024) to present, filterable by category (protocol/network/commerce/trust/crypto), alternating up/down. `.tl-track` (in `src/style.css`) uses generous top/bottom padding (170px) so expanded cards never get vertically clipped by `.tl-scroll`'s `overflow-y:hidden` — keep that padding (or more) if you touch this CSS. Additions are rare — roughly a few per month, not every run — but rare does NOT mean never: still check every run whether something genuinely major and dated happened, don't let "rare" become a standing excuse to stop looking entirely. Actually judge relevance rather than skipping a real one because it's easier not to look. Before adding: verify the date and fetch a real favicon (save it into `src/assets/` and reference it the same way the existing `.tl-logo` images do — never invent one, never reuse a generic monogram if a real icon is fetchable). Never apply `.badge-new` inside Timeline — it's excluded from the "new" system.
- **Key actors** (Home, `.actor-grid`): ~14 current major players with real logos and a one-line role each, explicitly a list that shifts over time — this block especially should reflect genuine shifts in who matters, not just re-list the same names out of habit. Refresh only when a player's role materially changes or a new one becomes clearly major; don't rewrite this block for minor news, but same principle as Timeline: rare doesn't mean never, still check.
- **badge-new** (`<span class="badge-new">`, coral dot before the title): mark ONLY items that are genuinely new since the *previous* run's edition — added during the routine daily scan. In scope: Home top-of-stack, This Fortnight, Network & Payments, Emerging Rails & Stablecoins, Commerce, Research, LinkedIn, Protocols. A story that's cross-listed on more than one page (e.g. on both Home and its category page, or both This Fortnight and a category page — meaning it appears in more than one `src/pages/*.html` file) gets the badge in EVERY file it appears in, not just the first/primary one. Never on Timeline (historical). Never on backfill/reorg work (adding older items to fill out a page, correcting a miscategorized item, etc.) — that's not "new since last edition" even if it's new to the page. See the CRITICAL RULE on badge-new hygiene above: clear badges from anything that was new in a prior run and is no longer the latest.
- Fime brand elements (`src/shell-top.html` + `src/footer.html` + `src/style.css`): sticky `.topbar` with the Fime logo (color/white pair, `src/assets/shell-1.png` / `shell-2.png`, swapped via `prefers-color-scheme`/`data-theme`, reuse as-is) linking to https://www.fime.com, the solid `.brand-band` turquoise strip, the mint `.site-footer` with "About this site" + "Made by Noam C. - agentic payment & commerce strategic consultant" + "© 2026 Fime. All rights reserved." — keep all three lines. Don't redesign CSS or swap colors outside the existing token set (navy #123940, coral #FF8564, mint #00E4C5, purple #7C7BAD). `nav.pages` uses `align-items:center` so the "More" dropdown button lines up with the plain nav links — keep that when touching the nav.
- Do NOT add PWA-only tags to `dist-artifact.html`/the live Artifact: no `<link rel="manifest">`, no `apple-touch-icon`/`apple-mobile-web-app-*` meta, no relative-path icon files — `build.js` already keeps those out of `dist-artifact.html` automatically (they only end up in `index.html` for GitHub Pages, via `src/head.html`). If you ever touch `build.js` itself, preserve that split. The Artifact's tab icon is set purely via the Artifact tool's `favicon` parameter (🛰️).
- **Numeric caps** (count-based, distinct from the age-based freshness window above): Home "Top of the stack" is capped at 10 items — when adding a new one, drop the oldest to stay at 10. Network & Payments, Emerging Rails & Stablecoins, Commerce, and Market are ALSO capped at roughly 10 items each, same pattern — when adding a new card would push a page past ~10, retire the oldest one(s) on that page to make room, don't just let it grow unbounded (the ~2-month freshness window is a backstop for staleness, this cap is about page length/scannability). LinkedIn is explicitly EXEMPT from any count cap — leave it sized by the freshness window alone, even if that means it runs well above 10 (confirmed by the user, don't trim it down to match the other pages). This Fortnight is also not count-capped, it's window-capped at 14 days (see the freshness rule) — it naturally runs somewhere around 20-30 rows, which is fine, not something to force smaller or larger.

## CRITICAL RULE on links
Before adding ANY new URL, verify it resolves (curl -s -o /dev/null -w "%{http_code}" -L -A "Mozilla/5.0" "<url>", or WebFetch). A 403 from a known corporate/publisher domain (Mastercard, SSRN, IMF, The Block, Perplexity, FIDO Alliance, e27, etc.) is normal bot-blocking, fine to keep. A 404 or connection failure means the URL is wrong, find the correct one via WebSearch or drop the item. Never construct a plausible-looking URL slug from memory, only use a URL you got directly from a search result or official page. Links can also go stale or get mis-attributed over time even after being correct once (e.g. a card pointing to a generic story instead of the specific one, or to the wrong date's article) — roughly every ~10 days (piggyback on the Archive cadence below), spend a pass re-checking a sample of EXISTING links across the `src/pages/*.html` files, not just newly added ones, and fix anything mismatched. See the CRITICAL RULE on link safety above for the security side of this check.

## CRITICAL RULE on numbers
Only add a numeric "Key figure" or market stat if the source states it with a precise, checkable definition (specific metric, timeframe/geography, one named source you can quote, linked via `<a class="stat">`). Skip generic "market size" or "% of commerce by year X" figures from templated aggregators. Prefer fewer, well-defined numbers over many vague ones.

## CRITICAL RULE on dates
Prefer a precise date over a vague one. If an entry only has a bare year ("2026"), actively try to find a more precise date via search before accepting the vague one (past example: two entries sat with bare "2026" for a while until a proper search turned up exact dates). Only fall back to a vague/undated placement (sorted to the bottom) if a genuine search effort turns up nothing more specific — don't guess a date that isn't stated by a source.

## CRITICAL RULE on tags/labels
No "China" tag/category anywhere on the site. Chinese and other Asian players (Alipay, WeChat Pay, UnionPay, etc.) get covered by real news, tagged by what they actually are (Network/Crypto/Commerce), never a geography-based tag. Same for Timeline and Fortnight's "By major player" framing if reintroduced: list the actor, not a geography/bloc. (This rule is about how to TAG Asian players once found — see the Geographic coverage rule above for actively finding them in the first place, the two are not the same rule.)

## Steps each run
1. Research fresh signals via WebSearch across ALL categories. Prioritize the last 2-3 days, but this is NOT a hard cutoff — this routine is triggered manually (not a guaranteed daily cron), so real multi-day gaps between runs happen. Recency should weight relevance and how you prioritize what to cover, not act as a strict filter that excludes something important just because it's older than a couple of days. If a genuinely major, relevant story surfaces from further back (up to roughly 14 days — matching This Fortnight's own retention window below, so anything you'd add is still eligible to live there) that hasn't already been covered on the site, include it, don't discard it. Beyond that ~14-day horizon, lean on judgment: only include something older if it's a real gap being caught late (e.g. the routine wasn't run for a while), not routine backfill for its own sake. Mix three kinds of searches, don't rely on named-entity/US-centric searches alone (see Anti-anchoring and Geographic coverage rules above), and remember these are floors not targets (see Floors, not targets rule):
   - Named-entity searches as a starting point: "agentic commerce" news, protocol updates (x402/AP2/ACP/UCP/MPP), major players (Visa, Mastercard, Stripe, OpenAI, Google, Amazon, PayPal, Apple, Klarna, Amex), crypto/stablecoin players (Coinbase, Binance, Circle, Ripple, Tether, Alipay), new arXiv/SSRN papers.
   - At least 2-3 open/generic searches with no company name, to surface players not yet on the site (new startups, regional PSPs, lesser-known protocols).
   - At least one Asia-focused and one Europe-focused search each run (see Geographic coverage rule), not only when something surfaces unprompted.
   Remember the Minimum coverage rule above: LinkedIn and general news are not "nice to have if found", make a real effort every run.
   For LinkedIn, PRIMARY method: run direct WebSearch queries like `site:linkedin.com/posts "agentic commerce"` or `site:linkedin.com/posts agentic payments [current month/year]` — these return the real `linkedin.com/posts/...` URL directly in the results, no resolution step needed, no wrapper problem. Use this as your main source, not a fallback.
   SECONDARY/supplementary only: the two Google News RSS feeds below can help surface story angles you might otherwise miss, but their article links are `news.google.com/rss/articles/...` wrappers that route through a Google consent page before reaching the real page — resolving those to a confirmed real URL is genuinely hard (empirically, well under half resolve with confidence via a follow-up search) and is exactly the kind of case the CRITICAL RULE on link safety tells you to leave out rather than guess. So: use these feeds for headlines/leads, then go find that same story via a direct LinkedIn search (or drop it) — do not rely on them as your only LinkedIn source for the run, since that will systematically starve this section (confirmed: a run that leaned on the RSS feeds alone added zero LinkedIn items despite the Minimum coverage rule). If a direct search genuinely turns up nothing for the window, that's when it's legitimately fine to report no new LinkedIn signal today — but only after trying the direct method first.
   - https://news.google.com/rss/search?q=site:linkedin.com+%22Agentic+Commerce%22&hl=en-US&gl=US&ceid=US:en
   - https://news.google.com/rss/search?q=%22Agentic+Commerce%22+(Visa+OR+Mastercard+OR+OpenAI+OR+Google+OR+Stripe)&hl=en-US&gl=US&ceid=US:en
   Tag LinkedIn items plainly as "LinkedIn", never "via Google News".
2. Update `src/pages/fortnight.html` (This Fortnight): add new dated rows (mark genuinely new ones with `.badge-new`, and the same badge in any other page file the story is cross-listed in), remove rows older than 14 days, sorted most-recent-first (vague "2026"-only dates go to the bottom per the CRITICAL RULE on dates above). While here, apply the CRITICAL RULE on description depth above to the "By major player" block: check each entry for a more recent significant move (this is a separate, unrestricted search, not part of the research window above).
3. Add or retire cards in `src/pages/network.html`, `crypto.html`, `commerce.html`, `market.html`, and `linkedin.html` as material signals appear — a handful of adds/removes per run, not a rebuild. Apply the CRITICAL RULE on freshness (retire anything past the ~2-month window) to all five. Apply the ~10-item numeric cap (see Numeric caps above) to `network.html`, `crypto.html`, `commerce.html`, and `market.html` only — NOT `linkedin.html`, which stays uncapped by count. In `src/pages/home.html`, refresh "Top of the stack" (capped at 10 items, most recent first — when adding a new one, drop the oldest to stay at 10) and "Key figures" if genuinely new precise numbers turn up.
4. Timeline (`src/pages/timeline.html`): only touch per the rule above (rare but not never — check every run, add only verified major milestones).
5. Research page (`src/pages/research.html`): arXiv + SSRN, last 2 months only, `.badge-new` on genuinely new papers.
6. Protocols page (`src/pages/protocols.html`): "New on Protocols" above "Major protocols" (stable, official spec link each, not subject to the freshness window). Categorize by what the thing actually is (a framework/primer publication → Protocols; a network actually shipping a feature → Network & Payments or Emerging Rails), not by who published it. A protocol/framework that's announced but not yet live can still be included, but label it clearly as such (e.g. "proposal, not yet launched, pending regulatory approval") rather than presenting it as already live.
7. Perplexity page (`src/pages/perplexity.html`): keep search-shortcut links current, no commentary blocks.
8. FACT page (`src/pages/fact.html`): keep "Competitive watch"/"Complementary technologies" current only on genuinely new, verifiable trust/evidence/authorization protocol news.
9. Archive page (`src/pages/archive.html`): snapshot roughly every 10 days (check the top entry's date). If 10+ days have passed: run the build, publish the freshly-built `dist-artifact.html` as a NEW standalone Claude Artifact (Artifact tool WITHOUT a url parameter), title "Watch - Agentic Commerce & Payments (Month Day, Year snapshot)", add a new top entry to `src/pages/archive.html` linking to it. This is also the trigger point for the periodic existing-links audit (see CRITICAL RULE on links) and a good moment to sweep for any vague-date entries that can now be upgraded (see CRITICAL RULE on dates). Otherwise skip all three.
10. Style: English only, no em dashes. Bump the "Edition of [date]" line in `src/pages/home.html` whenever you republish.
11. Before publishing: double-check the CRITICAL RULE on badge-new hygiene above has been applied across every page you touched (and any page cross-listing a story you touched). Then run `node build.js`, sanity-check the diff, then follow the Daily workflow steps 5-6 above (publish `dist-artifact.html` to the live URL, commit `src/` + generated files to git).

Only skip republishing entirely if, after a genuine search effort across ALL categories including This Fortnight/LinkedIn/news (per the Minimum coverage rule), nothing new and verifiable turns up within roughly the last 14 days — the 10-day archive check still runs regardless. Do not ask for confirmation before republishing or before committing to the local git repo (both are already approved as part of this routine).