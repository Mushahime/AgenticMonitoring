# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Local scratch
*.tmp

# Local-only routine backups (contains the weekly email recipient address - not for public repo).
# The daily refresh SKILL.md is safe and lives in the tracked routines/ folder instead.
/Skills(snapshots)/
